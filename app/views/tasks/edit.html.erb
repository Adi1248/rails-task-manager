

<%= form_for(@task, :as => :task, :url => update_path) do |t|%>

  <%= t.label :title %>
  <%= t.text_field :title %>


  <%= t.label :details %>
  <%= t.text_field :details  %>
  </div>

  <%= t.check_box :completed %>

  <%= t.submit %>

<% end %>
